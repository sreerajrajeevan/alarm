
// This file exists only to satisfy the file structure if needed, 
// but we prefer the root /challenge?id=... route for static exports.
export async function generateStaticParams() {
  return [];
}

export default function ChallengeDynamicPage() {
  return null;
}
